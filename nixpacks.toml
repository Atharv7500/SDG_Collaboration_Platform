[phases.setup]
nixPkgs = ["python3", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && python manage.py collectstatic --no-input", "cd backend && python manage.py migrate"]

[start]
cmd = "cd backend && gunicorn sdg_backend.wsgi:application"
